name: "Perf Data Reuse Manifest Daily Update"

# Regenerates `aic-core/src/aiconfigurator_core/systems/perf_data_reuse_manifest.yaml` from
# the perf data tree on a schedule and opens an automated PR if the committed
# file has drifted. Mirrors the support-matrix daily-run pattern: developers
# don't regenerate the manifest themselves on every data PR — the daily job
# folds drift back in.
#
# Manual runs: trigger via workflow_dispatch (optionally targeting a non-default
# branch).

on:
  schedule:
    # 7 AM PT / 2 PM UTC, same slot as the support matrix daily run.
    - cron: "0 14 * * *"
  workflow_dispatch:
    inputs:
      branch:
        description: "Target branch to run against (defaults to repo default branch)."
        required: false
        type: string
        default: ""

jobs:
  update-manifest:
    name: Regenerate perf_data_reuse_manifest.yaml
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write
      pull-requests: write
    env:
      TARGET_BRANCH: ${{ inputs.branch != '' && inputs.branch || github.event.repository.default_branch }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          ref: ${{ env.TARGET_BRANCH }}
          lfs: true

      - name: Git LFS Pull
        run: git lfs pull

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install manifest generator dependencies
        run: pip install "tqdm>=4.0.0" pandas pyarrow numpy pyyaml

      - name: Regenerate manifest
        run: |
          python3 tools/perf_database/generate_perf_data_reuse_manifest.py \
            --data-root aic-core/src/aiconfigurator_core/systems/data \
            --out-manifest aic-core/src/aiconfigurator_core/systems/perf_data_reuse_manifest.yaml

      # Daily archive of the perf-data sanity report (informational;
      # LFS is already pulled for the manifest step above).
      - name: Perf data sanity full sweep
        continue-on-error: true
        run: |
          python3 tools/perf_database/check_cross_backend.py \
            --out-md cross-backend-consistency.md \
            --out-json cross-backend-consistency.json

      - name: Upload consistency report artifact
        if: hashFiles('cross-backend-consistency.md') != ''
        uses: actions/upload-artifact@v7
        with:
          name: cross-backend-consistency
          path: |
            cross-backend-consistency.md
            cross-backend-consistency.json
          retention-days: 30

      - name: Detect drift
        id: drift
        run: |
          if git diff --quiet aic-core/src/aiconfigurator_core/systems/perf_data_reuse_manifest.yaml; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
          fi

      - name: Close existing PR if no changes
        if: steps.drift.outputs.has_changes == 'false'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PR_TITLE="fix: update perf_data_reuse_manifest [${{ env.TARGET_BRANCH }}]"
          EXISTING_PR=$(gh pr list --state open --search "in:title ${PR_TITLE}" --json number --jq '.[0].number' || echo "")
          if [ -n "$EXISTING_PR" ] && [ "$EXISTING_PR" != "null" ]; then
            echo "No changes detected. Closing existing PR #$EXISTING_PR"
            gh pr close "$EXISTING_PR" --comment "Closing: perf_data_reuse_manifest is now up to date with ${{ env.TARGET_BRANCH }}"
            git push origin --delete automated/update-perf-data-reuse-manifest-${{ env.TARGET_BRANCH }} 2>/dev/null || true
          fi

      - name: Create or Update Pull Request
        if: steps.drift.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          signoff: true
          author: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
          committer: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
          commit-message: "fix: update perf_data_reuse_manifest"
          title: "fix: update perf_data_reuse_manifest [${{ env.TARGET_BRANCH }}]"
          body: |
            Daily-run regeneration of `aic-core/src/aiconfigurator_core/systems/perf_data_reuse_manifest.yaml` from the perf data tree.

            Generated by [workflow run #${{ github.run_number }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}).
          branch: automated/update-perf-data-reuse-manifest-${{ env.TARGET_BRANCH }}
          base: ${{ env.TARGET_BRANCH }}
          delete-branch: true
          add-paths: |
            aic-core/src/aiconfigurator_core/systems/perf_data_reuse_manifest.yaml
          labels: |
            automated

      - name: Summary
        run: |
          if [ "${{ steps.drift.outputs.has_changes }}" == "true" ]; then
            echo "## Perf Data Reuse Manifest" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Changes detected. A PR has been created or updated against \`${{ env.TARGET_BRANCH }}\`." >> $GITHUB_STEP_SUMMARY
          else
            echo "## Perf Data Reuse Manifest" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "No changes detected. Manifest is up to date with \`${{ env.TARGET_BRANCH }}\`." >> $GITHUB_STEP_SUMMARY
          fi
