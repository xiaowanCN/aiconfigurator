# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

schema_version: 1
provenance: legacy
runtime:
  framework: vllm
  version: 0.24.0
tables:
  moe_perf:
    # 2026-08-12: appended 1,134 Nemotron-3 Super FP8 rows (hidden=1024,
    # inter=2688, 512 experts, topk=22) from auto-collector pipeline 62422303,
    # job 395118396. All 42 tasks completed with zero errors using
    # ModelOptFp8MoE -> FLASHINFER_CUTLASS -> FlashInferExperts.
    # 2026-08-02: appended 972 Kimi-K3 w4a16_mxfp4 rows (kernel_source
    # vllm_compressedtensorsw4a4mxfp4moe_marlin_marlinexperts_situ_as_silu)
    # collected on H100 in the STOCK v0.24.0 image via the
    # owner-approved situ-as-silu Marlin approximation + EP-local shim
    # (wave-3 pipeline 60730461, job 382178202, AIC revision 04036a56c); keys
    # disjoint from the base table. FULL EP 1..128 coverage — vllm's
    # marlin_moe_wna16 does NOT have the Hopper EP>1 crash that blocks
    # the sglang 0.5.14 marlin lane. Same 3 classified moe_tp=32 marlin
    # tile-limit failures ("Invalid thread config") as every other system.
    # 2026-08-09: appended 1053 MiniMax-M3 MoE shape rows (6144/3072, 128x4,
    # bf16, vllm_unquantizedfusedmoe_triton_tritonexperts; model-filtered
    # internal collection CI run, zero failures).
    status: complete
