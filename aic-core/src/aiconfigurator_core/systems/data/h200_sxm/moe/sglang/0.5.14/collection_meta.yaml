# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

schema_version: 1
provenance: legacy
runtime:
  framework: sglang
  version: 0.5.14
tables:
  moe_perf:
    # 2026-08-01: appended 488 Kimi-K3 w4a16_mxfp4 rows (3584/3072, 896x16,
    # sglang_marlin_moe, TP 1-32 x 3 distributions) — union of two runs at the
    # lock-fixed collector revision (487 + 1 collateral-only cell from run 1).
    # Every moe_ep_size > 1 K3 cell crashes with CUDA IMA (2590 cells) — the
    # known sglang 0.5.14 Hopper marlin EP-slice bug, candidate upstream issue.
    # 2026-08-04 (review): the K3 rows above are relabeled kernel_source
    # `sglang_marlin_moe_situ_as_silu`. K3's checkpoint activation
    # is SiTU; stock 0.5.14 has no SiTU path, so this lane benchmarks the
    # silu epilogue under the K3 w4a16_mxfp4 label — the same approximation
    # the vllm lane marks (epilogue delta up to ~5.5% at small token counts;
    # Blackwell trtllm-gen lanes are unaffected, same kernel + epilogue
    # constants). Re-collections on stock images must re-apply the suffix
    # until the sglang collector learns the checkpoint's situ property.
    # 2026-08-09: appended 3078 MiniMax-M3 MoE shape rows (6144/3072, 128x4,
    # bf16, sglang_fused_moe_triton; model-filtered internal collection CI
    # run, zero failures).
    # 2026-08-24: appended 3564 Nemotron-3-Super-120B-A12B moe rows
    # (hidden=1024, inter=2688, 512x22 experts; recollection campaign on
    # stock 0.5.14, AIC revision 1982d8d4) — restores the sglang-native
    # Nemotron-3 MoE lane after the 0.5.10 bridging dirs were pruned.
    # Keys disjoint from the base table. Nemotron-3-Ultra-FP8 generates
    # 0 sglang cases by design (allowed_modes: [] in the model case yaml).
    status: complete
