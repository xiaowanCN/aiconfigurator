# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

schema_version: 1
provenance: legacy
runtime:
  framework: sglang
  version: 0.5.14
# 2026-08-09: appended 3078 MiniMax-M3 MoE shape rows (6144/3072/128x4,
# model-filtered internal collection CI run, 8x B200, zero failures). The
# appended batch is 3078 unique keys, disjoint from prior rows; separately,
# the pre-existing table carried 7 internally duplicated legacy keys, whose
# stale copies were dropped keep-last during this merge (table-wide key
# uniqueness holds after the append).
# 2026-08-09: appended 3078 nvidia/MiniMax-M3-NVFP4 nvfp4 rows (model-filtered
# internal CI run, zero failures).
tables:
  moe_perf:
    # 2026-07-28: appended 3078 Kimi-K3 w4a16_mxfp4 rows (3584/3072, 896x16,
    # sglang_flashinfer_trtllm_moe, TP 1-32 x EP 1-128 x 3 distributions)
    # collected on 1x B200; zero failures, keys disjoint from prior rows.
    # 2026-08-01: appended 3078 Kimi-K3 w4a8_mxfp4_mxfp8 rows (same grid,
    # mxfp8 activations — the Blackwell serving default the SDK resolver
    # queries) collected on 8x B200 in the pinned stock 0.5.14 image;
    # zero failures, keys disjoint (dtype axis differs).
    # 2026-08-16: appended 3159 nvfp4 rows at the Qwen3.5-397B shape
    # (512 experts, 4096/1024, topk 10, sglang_flashinfer_trtllm_moe,
    # TP 1-32 x EP 1-256 x 3 distributions x 27 token counts), AIC-1716 —
    # same case plan as the gb300/gb200 2026-08-13 appends. Auto-collector
    # pipeline 62894161 on the PR #1519 branch (lmsysorg/sglang:v0.5.14-cu130);
    # zero failures, keys disjoint from prior rows.
    # 2026-08-26: replaced 54 rows (2 plans x 27 tokens) at the Qwen3.5-397B
    # nvfp4 shape — PR #1519 review healing of isolated latency spikes
    # (tp2/ep64 pl1.01, tp16/ep16 pl1.01). Scoped --case-filter
    # re-collection on 1x B200 (nsc-svg, lmsysorg/sglang:v0.5.14-cu130 —
    # the original grid's image), per-key MIN of two runs. Scoped checker
    # after: 0 same-plan >1.5x cells vs gb200 (was 2).
    status: complete
