# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

schema_version: 1
provenance: local
runtime:
  framework: vllm
  version: 0.1.dev19262
tables:
  # SM103 (GB300) vllm kda data from the 2026-08-02 wave-2 pipeline
  # (aic-auto-collector pipeline 60709585, job 382045468, AIC revision
  # b8f9ef27f). 1203 rows, image vllm/vllm-openai@sha256:e90e2603 (the
  # manifest kda-family pin). All six serving kernel paths present
  # (flashkda_fwd prefill, fused_kda_decode, fused_recurrent_kda chain
  # verify, Triton conv/packed pairs); lane structure identical to
  # b200_sxm/b300_sxm/gb200. 21 classified cell failures: 20 context
  # cells at the causal_conv1d int32 token-offset overflow guard
  # (total_tokens * proj >= 2**31) + 1 generation Triton bs=1024 grid
  # limit — the identical deterministic spectrum seen on every system.
  kda_perf:
    status: complete
