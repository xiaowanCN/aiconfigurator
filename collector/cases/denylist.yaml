# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# Hang denylist. The ONLY cases that belong here are ones that hang the
# collector or kill the node, so they cannot be handled by fail-fast +
# failure log.
#
# Ordinary crashes (illegal memory access, asserts, OOM) do NOT belong
# here: they fail fast, every failure is recorded and labeled with its
# (model, dtype) group, and systemic groups are surfaced in the collection
# summary (errors_by_group) as fix-me signals. There is no auto-skip —
# a failing group keeps running by design, so it gets fixed, not tolerated.
#
# Entry format (substring match against str(test_case)):
#   entries:
#     - contains: "tp=32, ep=16"
#       reason: "sglang moe_ep tp=32 deadlocks in NCCL init on <version>"
#       added: 2026-07-04
#
# Keep entries dated. Re-audit on every framework version bump and delete
# entries that no longer reproduce.

schema_version: 1

entries: []
